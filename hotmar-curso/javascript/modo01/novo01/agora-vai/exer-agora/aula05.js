var numero  = 5 // as quantidades de vezes que vc quer que ele epita.   

for(var vez = 0; vez < numero; vez++) // de onde ele vai iniciar. se a vez for menor que 5, e com o vez ++ vai aumentando td vez que for fazer a reação, ate chegar no numero 5 e assim parando.
{
console.log("executando o for, pela "+ vez + " vez")
}

console.log("acabou")

/*
o for vai funcionar assim

for repetição.

exeplicando a formula, primeiro vc determinar ate que numero vai fazer a conta, exeplo a variavel nuemro la em cima, informando o numero.

ja na formula, vc coloca de onde vai iniciar (var vez = 0) vai comersar com o zero, deepois ate que numero vai(vez < numero;-> lembrando que esse numero é 5) deppois a formular que vai fazer ele chergar ate esse numero proposto. (vez++) -> formula.
*/



